---
import '../styles/global.css';
import Navbar from '../../../shared/components/Navbar.astro';
import Footer from '../../../shared/components/Footer.astro';
import ThemeInit from '../../../shared/components/ThemeInit.astro';

interface Props {
  title: string;
  description?: string;
}

const { title, description = 'Browser-based Node.js runtime. Run Node.js in the browser with zero server dependency.' } = Astro.props;

const links = [
  { href: '/docs', label: 'Docs' },
  { href: '/compat', label: 'Are We Web Yet?' },
];
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
    <link rel="icon" type="image/png" sizes="48x48" href="/favicon-48x48.png" />
    <link rel="icon" type="image/png" sizes="192x192" href="/favicon-192x192.png" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <ThemeInit />
  </head>
  <body>
    <slot name="navbar">
      <Navbar links={links} />
    </slot>

    <main>
      <slot />
    </main>

    <slot name="footer">
      <Footer />
    </slot>
  </body>
</html>
