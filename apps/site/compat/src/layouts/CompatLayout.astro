---
import '../styles/global.css';
import '../styles/compat.css';
import Navbar from '../../../shared/components/Navbar.astro';
import ThemeInit from '../../../shared/components/ThemeInit.astro';

interface Props {
  title?: string;
}

const { title = 'Compatibility' } = Astro.props;
const links = [{ href: '/docs', label: 'Docs' }];
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <ThemeInit />
    <slot name="head" />
  </head>
  <body>
    <Navbar links={links} />
    <main class="container compat-page">
      <slot />
    </main>
  </body>
</html>
